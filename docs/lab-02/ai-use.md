# Lab 2 — AI Use and Reflection

**LLM/agent used:** Antigravity AI Coding Agent (Gemini 3.7 Flash)

---

## Selected Key Prompts (Sprint 2 / Issues 5–9)

| #   | Prompt (summarised)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | What I did with the result                                                                                                                                                                                                                                                                                                                                                            |
| :-- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Draft Sprint 2 Specification (Issue 5):** Formulate the complete engineering specification for Issue 5 across 4 documents (`specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`) incorporating Zen Green design tokens, dirty form guard, pre-upload staging, and soft-removal audit rules.                                                                                                                                                                                                                                                                    | Reviewed architectural feasibility, verified all 12 Functional Requirements and 11 Business Rules with peer reviewer, and confirmed Prisma schema models.                                                                                                                                                                                                                             |
| 2   | **Standardize REST API Contracts & Status Codes (Issue 5):** Define standard JSON success/error envelopes with `correlationId` and `fieldErrors`, error codes, and complete endpoint specs for `GET /api/requesters`, `GET /api/categories`, `GET /api/related-systems`, `POST /api/attachments/pre-upload`, `POST /api/tickets`, `GET /api/tickets`, `GET /api/tickets/:id`, `GET /api/attachments/:id/download`, and `DELETE /api/attachments/:id`.                                                                                                                   | Verified HTTP status code usage (200, 201, 400, 403, 404, 410, 413, 415, 422, 500) and ensured data isolation scoping by `X-Requester-Id`.                                                                                                                                                                                                                                            |
| 3   | **Define Zen Green UI & WCAG AA Accessibility (Issue 5):** Create comprehensive UI guidelines covering the Zen Green palette (`#006B3C`, `#0B7A46`, `#EAF6EF`), color-independent badge glyphs, responsive layouts (Desktop $\ge 992$px table, Mobile $< 768$px stacked cards), and WCAG 2.1 AA keyboard/screen reader requirements.                                                                                                                                                                                                                                    | Validated high-contrast color combinations, modal focus-trapping rules, ARIA live-region declarations for dynamic uploads, and visual checklist items.                                                                                                                                                                                                                                |
| 4   | **Establish Test Engineering Strategy & Traceability (Issue 5):** Build the multi-tiered test strategy (Vitest for UI & Unit, Supertest for API Integration, Playwright for E2E) and construct the Acceptance Criteria Traceability Matrix mapping AC-01 through AC-12 directly to test identifiers.                                                                                                                                                                                                                                                                    | Validated test coverage matrix, ensured all 12 acceptance criteria have automated test mappings, and clarified planned test scope for Sprint 2 TDD implementation.                                                                                                                                                                                                                    |
| 5   | **Implement Requester Context, Prisma Models, Seed & API Endpoints (Issue 6):** Define Prisma models (`RequesterUser`, `Category`, `RelatedSystem`, `Ticket`, `Attachment` with `uploadedBy` relation), apply migrations, create idempotent seed script (4 active, 1 inactive requester, 4 categories, 6 related systems), build `GET /api/requesters`, `GET /api/related-systems`, `GET /api/tickets`, `RequesterContext` with `localStorage` persistence, Zen Green `Header`, `DirtyGuardModal`, and comprehensive Supertest & Vitest test suites.                    | Verified database schema migrations and foreign key constraints, ensured seed idempotency and count verification in CI, validated active-requester filtering, confirmed requester-specific ticket state clearing and reloading upon context switch, and verified all unit and integration tests passed.                                                                               |
| 6   | **Build Ticket Creation Form, Atomic Creation API & Pre-Upload Attachments (Issue 7):** Implement `multer` upload middleware (`POST /api/attachments/pre-upload`) with 5MB limit and strict MIME validation (JPEG, PNG, WEBP, PDF), atomic ticket creation (`POST /api/tickets`) with sequential `TKT-YYYY-NNNNN` numbering and transactional attachment linking, Zen Green `CreateTicketForm` component with dynamic cascading dropdowns, character counters, priority pills (P0–P3), drag-and-drop file staging area, inline error mapping, and dirty state tracking. | Verified atomic database transaction and sequential ticket number incrementing via Supertest, cleanly resolved merge conflicts with `lab2-staging`, refined client-side attachment validation to strictly enforce both file extensions and MIME types (`file.type`) following peer review feedback, and confirmed all 20 backend and 14 frontend tests passed with zero build errors. |
| 7   | **Develop My Tickets Dashboard, Filtering/Pagination & Section 8.1 Requester Selection (Issue 8):** Implement `MyTicketsDashboard` adhering to Zen Green design guidelines with responsive dual-view (Desktop `<table>` $\ge 992$px vs Mobile stacked `.zg-ticket-card` $< 768$px), multi-field search and taxonomy filters (Category, Requested Priority, Status), sorting controls, server-side pagination (10/page), Section 8.1 Development Requester Selection screen with breadcrumb hierarchy, conditional header navigation rendering, 5-second auto-dismissing ticket creation banner, table text truncation with tooltip, and comprehensive Vitest and Supertest test suites. | Verified strict requester data isolation (`X-Requester-Id`), confirmed debounced search and filter parameter mapping with server-side pagination, validated responsive table/card layout transitions without horizontal scroll, ensured auto-dismiss timer lifecycle and manual dismissal cleanup, and verified all 37 backend and 36 frontend tests passed. |
| 8   | **Implement Attachment Soft-Removal & E2E Validation (Issue 9):** _(Planned for Issue 9)_                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | To be executed during Issue 9 branch implementation.                                                                                                                                                                                                                                                                                                                                  |

---

## Reflection
